<UserControl x:Class="FCZ.App.Controls.LogEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="10">
        <TextBlock Text="Log" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="ID:" VerticalAlignment="Center" Margin="0,2"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Id}" Margin="5,2"/>
            <Label Grid.Row="1" Grid.Column="0" Content="Message:" VerticalAlignment="Top" Margin="0,2"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Message}" AcceptsReturn="True" TextWrapping="Wrap" Height="60" Margin="5,2"/>
        </Grid>
    </StackPanel>
</UserControl>


